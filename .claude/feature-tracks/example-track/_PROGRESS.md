# Example Track — Progress Log

Human-readable, append-only progress log for this track. The `progress-tracker` skill writes here in lockstep with `_STATUS.json` — don't hand-edit either; route through the orchestrator (`/build` for the spine track, `/feature example-track` for any track).

This is a placeholder track shipped with the starter so the build system is runnable immediately. Replace it with a real track.

<!-- entries below this line -->
