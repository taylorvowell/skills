---
description: Walk through pre-deploy checks before shipping to Vercel
---

This command will be built out once the deploy process is mature.

For now: run the project's lint, typecheck, and test scripts with your package manager. Show git status. Confirm no .env files are staged. Show what's about to deploy.
